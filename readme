# Assignment #5 — 3D Geometry Processing with Open3D

Этот проект реализует все 7 шагов ассайнмента по работе с 3D-геометрией в библиотеке **Open3D**.  
Каждый шаг визуализируется в отдельном окне, а в консоль выводится статистика: количество вершин, треугольников, наличие цвета, нормалей, количество вокселей и координаты экстремумов.

## 📌 Используемые технологии
- Python 3.x
- Open3D
- NumPy
- Matplotlib

## 📂 Файлы проекта
- `5assn.py` — основной скрипт, содержащий все 7 шагов.
- `ufo.obj` — уникальная 3D-модель (требование задания).
- (опционально) `README.md`

## 🧩 Этапы работы (Шаги задания)

### **1. Загрузка и визуализация**
- Загружает 3D-модель (`ufo.obj`)
- Нормализует по размеру и центру
- Показывает модель
- Выводит: вершины, треугольники, наличие цвета и нормалей

### **2. Преобразование в облако точек**
- Конвертация модели в point cloud (`40 000` точек)
- Визуализация
- Вывод: количество точек, цвета, нормали

### **3. Poisson-реконструкция поверхности**
- Построение треугольного mesh из point cloud
- Обрезка по bounding box
- Цветная визуализация (нормали → цвета)
- Вывод: вершины, треугольники, наличие цвета

### **4. Вокселизация**
- Создание `VoxelGrid` с шагом `0.03`
- Визуализация
- Вывод: количество вокселей, параметры

### **5. Добавление плоскости**
- Создание вертикальной плоскости
- Размещение рядом с моделью
- Совместная визуализация

### **6. Вертикальный клиппинг**
- Реализация математического отсечения точек по плоскости
- Вывод: количество оставшихся точек, нормали, цвета

### **7. Градиент и экстремумы**
- Применение цветового градиента по оси Z
- Нахождение минимальной и максимальной точки
- Пометка экстремумов маленькими кубами
- Вывод координат

## 🚀 Запуск проекта

Установите зависимости:

```bash
pip install open3d numpy matplotlib
